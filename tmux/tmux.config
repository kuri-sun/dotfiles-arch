# reload
bind-key -T prefix r source-file ~/.config/tmux/tmux.config \; display-message 'reload was succesful.'

set -g prefix C-b

# set the index from 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows set-option

# vim bindings
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Theme
source-file ~/.config/tmux/yoda.tmux
